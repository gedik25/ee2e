# Kopyala: cp .env.example .env  (ve değerleri değiştir)

# Postgres
POSTGRES_PASSWORD=change-me-to-a-long-random-string

# Flask
FLASK_SECRET=change-me-to-another-long-random-string
LOG_LEVEL=INFO

# CORS — Flutter app schema'ları (virgülle ayrılmış); * sadece dev için
CORS_ORIGINS=*
